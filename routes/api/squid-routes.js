const router = require('express').Router();
const {
    getAllSquid,
    getSquidById,
    createSquid,
    deleteSquid
} = require ('../../controllers/') // so would i need a squid controller?

// Set up GET at /api/squids

// Set up GET by id at /api/squids/:id

// Set up POST at /api/squids

// Set up DELETE at /api/squids/:id